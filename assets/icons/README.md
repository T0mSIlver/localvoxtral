# Icon Assets

- `assets/icons/app/AppIcon.png`: source image for the packaged macOS app icon (`.icns` is generated during packaging).
- `assets/icons/menubar/MenubarIcon.svg`: source SVG for the macOS top bar icon (`MenubarIconTemplate.png` is generated during packaging).
