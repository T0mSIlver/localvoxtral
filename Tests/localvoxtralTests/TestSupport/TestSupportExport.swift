// The doubles that need only the core live in Tests/localvoxtralTestSupport,
// which the core's suite shares (#616). Re-exported so every file here sees
// them, as it did when they lived in this directory.
@_exported import localvoxtralTestSupport
