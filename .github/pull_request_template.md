## What

<!-- What changes and why. -->

## Proof

<!-- Real command output, not claims. Delete lines that don't apply.
     "CI is green" alone is not proof for a behavior change. -->

- [ ] Unit suite green — the PR's hosted `build-test` run (link it)
- [ ] Live lane a rule requires — its summary line from the PR's CI run
- [ ] Bug fix: regression test added — failing run before the fix and passing run after (paste both)
- [ ] UI change: what was verified by hand, and how
